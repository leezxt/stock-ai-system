services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: stock_ai
      POSTGRES_USER: stock_ai
      POSTGRES_PASSWORD: ${STOCKAI_DATABASE_PASSWORD:-stock_ai}
    ports:
      - "${STOCK_AI_POSTGRES_PORT:-5432}:5432"
    volumes:
      - stock_ai_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U stock_ai -d stock_ai"]
      interval: 5s
      timeout: 3s
      retries: 20

  stock-ai-app:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${STOCK_AI_HOST_PORT:-18080}:8080"
    environment:
      STOCKAI_DATABASE_URL: jdbc:postgresql://postgres:5432/stock_ai
      STOCKAI_DATABASE_USERNAME: stock_ai
      STOCKAI_DATABASE_PASSWORD: ${STOCKAI_DATABASE_PASSWORD:-stock_ai}
      STOCKAI_AUTH_SECRET: ${STOCKAI_AUTH_SECRET:-}
      STOCKAI_SECRETS_ENCRYPTION_KEY: ${STOCKAI_SECRETS_ENCRYPTION_KEY:-}
      STOCKAI_GOOGLE_CLIENT_ID: ${STOCKAI_GOOGLE_CLIENT_ID:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-5.5}
      STOCKAI_RAG_EMBEDDING_PROVIDER: ${STOCKAI_RAG_EMBEDDING_PROVIDER:-hash}
      STOCKAI_RAG_EMBEDDING_API_KEY: ${STOCKAI_RAG_EMBEDDING_API_KEY:-}
      STOCKAI_RAG_EMBEDDING_URL: ${STOCKAI_RAG_EMBEDDING_URL:-https://api.openai.com/v1/embeddings}
      STOCKAI_RAG_EMBEDDING_MODEL: ${STOCKAI_RAG_EMBEDDING_MODEL:-text-embedding-3-small}
      STOCKAI_RAG_EMBEDDING_DIMENSION: ${STOCKAI_RAG_EMBEDDING_DIMENSION:-16}
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
      GEMINI_MODEL: ${GEMINI_MODEL:-gemini-2.0-flash}
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
      DEEPSEEK_MODEL: ${DEEPSEEK_MODEL:-deepseek-chat}
      MIMO_API_KEY: ${MIMO_API_KEY:-}
      MIMO_MODEL: ${MIMO_MODEL:-mimo-v2.5-pro}
      MIMO_CHAT_COMPLETIONS_URL: ${MIMO_CHAT_COMPLETIONS_URL:-https://api.xiaomimimo.com/v1/chat/completions}
      ALPHAVANTAGE_API_KEY: ${ALPHAVANTAGE_API_KEY:-}
      FINMIND_API_TOKEN: ${FINMIND_API_TOKEN:-}
      FINMIND_BASE_URL: ${FINMIND_BASE_URL:-https://api.finmindtrade.com/api/v4}
      FMP_API_KEY: ${FMP_API_KEY:-}
      STOCKAI_TWSE_DISCLOSURE_URL: ${STOCKAI_TWSE_DISCLOSURE_URL:-}
      STOCKAI_WATCHLIST_ALERTS_SCHEDULER_ENABLED: ${STOCKAI_WATCHLIST_ALERTS_SCHEDULER_ENABLED:-false}
      STOCKAI_WATCHLIST_ALERTS_SCHEDULER_FIXED_DELAY_MS: ${STOCKAI_WATCHLIST_ALERTS_SCHEDULER_FIXED_DELAY_MS:-900000}
      STOCKAI_WATCHLIST_ALERTS_SCHEDULER_INITIAL_DELAY_MS: ${STOCKAI_WATCHLIST_ALERTS_SCHEDULER_INITIAL_DELAY_MS:-60000}
      STOCKAI_WATCHLIST_ALERTS_NOTIFICATION_MODE: ${STOCKAI_WATCHLIST_ALERTS_NOTIFICATION_MODE:-LOCAL_ONLY}
    volumes:
      - stock_ai_data:/app/backend/data

volumes:
  stock_ai_data:
  stock_ai_postgres:
